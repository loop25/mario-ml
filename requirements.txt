# ============================================================================
# Super Mario Bros ML Training Project - Dependencies
# ============================================================================
# Python 3.11 required (3.13 not compatible with gym-super-mario-bros)
# Install: pip install -r requirements.txt
#
# GPU ACCELERATION (recommended):
#   For NVIDIA GPUs, install PyTorch with CUDA instead of the default CPU build:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   RTX 50-series (Blackwell/sm_120) requires cu128 or newer.
#   See https://pytorch.org/get-started/locally/ for other CUDA versions.
# ============================================================================

# --- Game Environment ---
gym==0.26.2                    # OpenAI Gym (compatible version for gym-super-mario-bros)
gym-super-mario-bros==7.4.0    # Super Mario Bros environment (includes built-in NES emulation)
nes-py==8.2.1                  # NES emulator backend for gym-super-mario-bros

# --- Machine Learning Core ---
numpy>=1.24,<2.0              # Numerical computing (numpy 2.0 has breaking changes)
torch>=2.0                     # PyTorch - DQN/PPO neural networks (install CUDA build for GPU)
torchvision>=0.15              # PyTorch vision utilities

# --- Reinforcement Learning ---
stable-baselines3>=2.0,<2.4   # PPO implementation (includes CNN policies)
shimmy>=0.2.1,<1.0             # Compatibility layer: old gym envs -> gymnasium (required by SB3)
neat-python>=0.92              # NEAT neuroevolution algorithm

# --- Visualization ---
pygame>=2.5                    # Game rendering and dashboard window
matplotlib>=3.7                # Real-time graphs (rendered to pygame surfaces)

# --- Utilities ---
opencv-python>=4.8             # Frame preprocessing (resize, grayscale) + video recording
pyyaml>=6.0                    # Config file loading (YAML format)
tqdm>=4.65                     # Progress bars for training loops
